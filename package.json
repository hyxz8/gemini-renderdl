{
  "name": "reverse-proxy-server",
  "version": "1.0.0",
  "description": "A simple reverse proxy server to forward requests to https://generativelanguage.googleapis.com and remove privacy-related headers.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "reverse-proxy",
    "http-proxy-middleware",
    "privacy",
    "express"
  ],
  "author": "Your Name",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "http-proxy-middleware": "^2.0.6"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
